<UserControl x:Class="SimpleDICOMToolkit.Views.PrintPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:v="clr-namespace:SimpleDICOMToolkit.Views"
             mc:Ignorable="d" 
             d:DesignWidth="340"
             d:DesignHeight="420">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Button ToolTip="Open File"
                    s:View.ActionTarget="{Binding}"
                    Command="{s:Action OpenImage, ActionNotFound=Disable}">
                <Image Width="20" Height="20" Stretch="Fill" 
                       Source="pack://application:,,,/Icons/openfile.png" />
            </Button>
            <TextBox Text="{Binding DcmFile, Mode=OneWay}" Width="272" Height="24" IsReadOnly="True" />
        </StackPanel>
        <Image Grid.Row="1" Source="{Binding ImageSource}" />
    </Grid>
</UserControl>
